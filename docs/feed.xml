<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/feed.xml" rel="self" type="application/atom+xml"/><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-15T09:55:50+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/feed.xml</id><title type="html">Tejas Stanley: GSOC X JDE Robotics 2026</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Coding Period Week 7</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-7/" rel="alternate" type="text/html" title="Coding Period Week 7"/><published>2026-07-10T00:00:00+00:00</published><updated>2026-07-10T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-7</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-7/"><![CDATA[<p>This week, the main focus was to extend the GUI support for LiDAR segmentation using SemanticKITTI.</p> <p>In the previous week, I had started a first Plotly-based LiDAR visualization experiment. This week I moved the LiDAR part closer to a complete GUI flow with dataset viewing, inference, and evaluation.</p> <h2 id="lidar-segmentation-dataset-viewer">LiDAR Segmentation Dataset Viewer</h2> <p>The next main part of the week was the LiDAR segmentation GUI.</p> <p>The dataset viewer now uses the SemanticKITTI dataset structure and lets the user select a split and frame from the GUI. The selected frame is rendered as an interactive 3D point cloud using Plotly.</p> <p>I added support for two coloring modes:</p> <ul> <li>semantic label colors;</li> <li>intensity-based colors.</li> </ul> <p>The intensity values come from the <code class="language-plaintext highlighter-rouge">.bin</code> point cloud file itself. SemanticKITTI point clouds store each point as <code class="language-plaintext highlighter-rouge">x, y, z, intensity</code>, so intensity is an input feature from the LiDAR data, not a model output.</p> <p>When labels are available, the viewer can color points by class, A class table has been added to display the color mapping.</p> <p>For the Plotly view, I added a top-view camera option. This makes it easier to reset the visualization to a useful view when the user rotates or zooms around the point cloud. I also added hover text so the user can see point coordinates and class names when label information is available.</p> <h2 id="lidar-segmentation-inference">LiDAR Segmentation Inference</h2> <p>The model wiring follows the SemanticKITTI tutorial. The GUI loads the model using the required model configuration, checkpoint, ontology, and PerceptionMetrics model configuration. Once the model is loaded, the inference tab can run prediction on a selected <code class="language-plaintext highlighter-rouge">.bin</code> point cloud.</p> <p>The prediction is then visualized in the same Plotly point cloud viewer. This made it possible to compare the raw point cloud and the predicted semantic classes from inside the GUI.</p> <p>One thing I had to handle carefully was how Streamlit file uploads work. Uploaded files are stored in memory, while the PerceptionMetrics LiDAR model prediction path expects a local file path. Because of that, the uploaded <code class="language-plaintext highlighter-rouge">.bin</code> file is temporarily written to disk before calling the model prediction function.</p> <p><img src="/gsoc2026-Tejas_Stanley/assets/img/lidar_seg_infer.png" alt="LiDAR segmentation inference preview" style="max-width: 100%; margin-bottom: 1.5rem;"/></p> <h2 id="lidar-segmentation-evaluation">LiDAR Segmentation Evaluation</h2> <p>I also started wiring the evaluator tab for SemanticKITTI LiDAR segmentation.</p> <p>The evaluator loads the dataset, checks that the model is available, and calls the model <code class="language-plaintext highlighter-rouge">eval()</code> function from PerceptionMetrics.</p> <p>I added progress callbacks for LiDAR evaluation as well. This allows the GUI to show live progress while evaluation is running, instead of only showing results after the full dataset has finished.</p> <p>The evaluator also supports ontology translation, since SemanticKITTI labels, train IDs, and model outputs may not always directly match. This follows the same idea used in the segmentation tutorials.</p> <p><img src="/gsoc2026-Tejas_Stanley/assets/img/lidar_seg_eval.png" alt="LiDAR segmentation evaluation preview" style="max-width: 100%; margin-bottom: 1.5rem;"/></p> <h2 id="next-steps">Next Steps</h2> <p>The next step is to test the LiDAR segmentation GUI more carefully with SemanticKITTI and clean up the code before opening a separate pull request for it.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="gui"/><category term="segmentation"/><category term="semantickitti"/><category term="lidar"/><summary type="html"><![CDATA[Adding LiDAR segmentation GUI support with SemanticKITTI]]></summary></entry><entry><title type="html">Coding Period Week 6</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-6/" rel="alternate" type="text/html" title="Coding Period Week 6"/><published>2026-07-03T00:00:00+00:00</published><updated>2026-07-03T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-6</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-6/"><![CDATA[<p>This week I continued working on the PerceptionMetrics GUI. The main focus was to make the task-based GUI structure more useful for image segmentation, while also starting a first experiment for LiDAR segmentation visualization.</p> <p>In the previous weeks, the GUI was split into task folders so that image detection, image segmentation, and LiDAR segmentation could have their own sidebar, dataset viewer, inference tab, and evaluator tab. This week I worked more on filling in the image segmentation parts.</p> <h2 id="pull-requests">Pull Requests</h2> <p>The main pull request related to this week’s GUI work is:</p> <ul> <li>Image segmentation GUI: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/609/">JdeRobot/PerceptionMetrics#609</a></li> </ul> <h2 id="image-segmentation-gui">Image Segmentation GUI</h2> <p>I first continued with the image segmentation dataset viewer. Cityscapes and nuImages were the datasets I focused on. Other datasets can be added later by adding the required sidebar inputs and dataset viewer logic in the <code class="language-plaintext highlighter-rouge">sidebar.py</code> and <code class="language-plaintext highlighter-rouge">dataset_viewer.py</code> files.</p> <p>The viewer now uses the dataset path and dataset-specific inputs from the sidebar. For Cityscapes, this includes the image directory, label directory, image suffix, label suffix, and whether train IDs are being used. For nuImages, this includes the dataset version and the generated labels directory.</p> <p>The GUI shows the selected image along with the ground-truth mask overlay. This helped in checking whether the labels were being read correctly and whether the mask was aligned with the image.</p> <p>I also connected nuImages segmentation to the dataset viewer. For nuImages, the GUI uses the dataset version and the generated labels directory from the sidebar.</p> <h2 id="model-inputs-for-segmentation">Model Inputs for Segmentation</h2> <p>I worked on the model input part of the image segmentation sidebar. The model input can either be a model file path or a Hugging Face model folder. Since the current Cityscapes and nuImages examples use Hugging Face SegFormer models, I focused on making sure the GUI can handle those models correctly. The basic option for Torch model files is present, but the main tested flow for now is the Hugging Face model folder path. Support for other <code class="language-plaintext highlighter-rouge">.pt</code> or <code class="language-plaintext highlighter-rouge">.pth</code> model-loading cases can be extended with small changes in the model loading function.</p> <p>So the sidebar now asks for:</p> <ul> <li>model path or Hugging Face model folder;</li> <li>model config JSON;</li> <li>model ontology JSON.</li> </ul> <p>This follows the same idea as the tutorials, where the model is wrapped by the PerceptionMetrics model class using a config file and an ontology file.</p> <p>For Hugging Face SegFormer models, I checked how the downloaded model folder and config files should be passed.</p> <h2 id="inference-and-evaluation">Inference and Evaluation</h2> <p>I also worked on the image segmentation inference and evaluator tabs.</p> <p>For inference, the GUI lets the user upload an image and run prediction using the loaded segmentation model. The prediction is then visualized using the model ontology, similar to how the tutorial visualizes predictions. It is very similar to the inference tab of image detection.</p> <p>For evaluation, the GUI uses the same <code class="language-plaintext highlighter-rouge">model.eval()</code> path from PerceptionMetrics. Similar to the tutorials and the evaluation tab of image detection.</p> <p>I also added support for ontology translation in the evaluator. This is important for datasets like Cityscapes and nuImages, where the dataset labels and model output labels may not always use the same IDs.</p> <p>One issue that came up while testing was Streamlit session state. I tried simplifying some browse buttons by removing small callback functions, but Streamlit does not allow changing a widget key after the widget has already been created in the same run. Because of that, browse buttons that update text input fields still need callback functions.</p> <h2 id="lidar-segmentation-viewer-experiment">LiDAR Segmentation Viewer Experiment</h2> <p>As suggested by David and Praveena in the previous week, I also started a small experiment for LiDAR segmentation visualization using SemanticKITTI.</p> <p>The viewer scans the SemanticKITTI folder structure, lists available frames by split, and renders the selected point cloud. It can also use the SemanticKITTI YAML config to color points by labels when label files are available.</p> <p>I first wanted to try Open3D, but for embedding inside Streamlit, I used a Plotly-based 3D view for now. This gives an interactive point cloud directly in the browser and is easier to test inside the current GUI.</p> <p>The Plotly-based viewer supports panning, rotation, and zooming. It also shows the point coordinates when hovering over the point cloud. This makes it useful for quickly checking whether a SemanticKITTI frame is being loaded and rendered correctly.</p> <p>One issue I faced was that the Plotly viewer needs WebGL support in the browser. On my system, I had to enable <code class="language-plaintext highlighter-rouge">Override software rendering list</code> in Chrome settings before the 3D point cloud rendered properly.</p> <p>As discussed with David and Praveena, the visualization can also be extended later to provide rendered images from fixed views, such as top view and front view. These views could be useful for evaluation and visualization inside the GUI.</p> <p>The maximum number of rendered points can also be controlled from the sidebar. I kept the default at 50K points. I tried 100K points as well, but the browser started to lag, so 50K is a better default for now. This limit can be increased later if needed.</p> <p><img src="/gsoc2026-Tejas_Stanley/assets/img/LIDAR_plotly_render.png" alt="LiDAR segmentation dataset viewer preview" style="max-width: 100%; margin-bottom: 1.5rem;"/></p> <h2 id="next-steps">Next Steps</h2> <p>I will be completing the LiDAR segmentation viewer and adding inference and evaluation support for SemanticKITTI.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="gui"/><category term="segmentation"/><category term="cityscapes"/><category term="nuimages"/><category term="semantickitti"/><category term="lidar"/><summary type="html"><![CDATA[Extending the PerceptionMetrics GUI for image segmentation and starting LiDAR visualization]]></summary></entry><entry><title type="html">Coding Period Weeks 4 and 5</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-4-5/" rel="alternate" type="text/html" title="Coding Period Weeks 4 and 5"/><published>2026-06-26T00:00:00+00:00</published><updated>2026-06-26T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-4-5</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-4-5/"><![CDATA[<p>During weeks 4 and 5 of the coding period, I worked on two main things in PerceptionMetrics. First, I looked at how segmentation evaluation can work when the model labels and dataset labels do not directly match. Second, I started refactoring the GUI so image detection, image segmentation, and LiDAR segmentation can be handled as separate tasks.</p> <p>This work came after the nuImages segmentation tutorial and the SemanticKITTI tutorial from the previous weeks.</p> <p>I also had to take a one-week leave of absence during this period to complete my exams. I informed my mentors about this in advance, and since the project progress was already ahead of the expected pace, we agreed that this would be manageable without affecting the overall timeline.</p> <h2 id="pull-requests">Pull Requests</h2> <p>The main pull requests related to the tutorial updates are:</p> <ul> <li>nuImages segmentation tutorial: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/599">JdeRobot/PerceptionMetrics#599</a></li> <li>Segmentation evaluation and tutorial updates: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/605">JdeRobot/PerceptionMetrics#605</a></li> </ul> <h2 id="evaluation-and-ontology-translation">Evaluation and Ontology Translation</h2> <p>I looked at the <code class="language-plaintext highlighter-rouge">eval</code> function for segmentation models and how it handles different label spaces. Earlier tutorials used the dataset <code class="language-plaintext highlighter-rouge">export</code> function to store labels in a different ontology before evaluation. This works, but evaluation should not always need a dataset export just because the model and dataset use different label IDs.</p> <p>While checking this, I found that ontology translation can help avoid this extra export step. Instead of saving another version of the dataset, evaluation can use a translation ontology or lookup table directly.</p> <p>This is useful for datasets such as:</p> <ul> <li>nuImages image segmentation</li> <li>Cityscapes image segmentation</li> <li>SemanticKITTI LiDAR segmentation</li> </ul> <p>In these datasets, the raw dataset labels, train IDs, model output IDs, and ignored classes may differ. The main point is that ontology translation can be used during evaluation, while dataset export is still useful when converted labels need to be saved on disk.</p> <h2 id="starting-the-gui-refactor">Starting the GUI Refactor</h2> <p>The other major part of the work was the PerceptionMetrics GUI. The existing GUI had three tabs:</p> <ul> <li>Dataset Viewer</li> <li>Inference</li> <li>Evaluator</li> </ul> <p>This worked for image detection, but it would become messy once image segmentation and LiDAR segmentation were added. Each task needs different dataset inputs, model inputs, visualizations, and evaluation options.</p> <p>To make this easier to extend, I started separating the GUI by task. The structure now moves toward folders like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tabs/tasks/image_detection/
tabs/tasks/image_segmentation/
tabs/tasks/lidar_segmentation/
</code></pre></div></div> <p>Each task can then have its own:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sidebar.py
dataset_viewer.py
inference.py
evaluator.py
</code></pre></div></div> <p>This keeps image detection code separate from segmentation code. It also gives us a cleaner place to add LiDAR visualization and evaluation later.</p> <h2 id="preserving-the-image-detection-workflow">Preserving the Image Detection Workflow</h2> <p>Before adding new segmentation behavior, I made sure image detection still behaved like the original GUI. I moved the existing image detection dataset viewer, inference tab, evaluator tab, and sidebar into the new task-specific folder while keeping the implementation close to the original version.</p> <p>This was important because the GUI refactor should not silently change the existing detection workflow. The goal was to first move the code into a better structure, not redesign everything at once.</p> <p>The image detection viewer still supports COCO and YOLO-style datasets. The sidebar remains responsible for dataset selection, model loading, configuration, and evaluation parameters.</p> <h2 id="image-segmentation-dataset-viewer">Image Segmentation Dataset Viewer</h2> <p>After the task split, I started implementing the image segmentation side with Cityscapes first. Other datasets such as nuImages, Wildscenes, RUGD, Rellis3D, and GOOSE are still planned, but Cityscapes was a good starting point because the tutorial already gives a clear reference workflow.</p> <p>The first implemented part was the Cityscapes dataset viewer. It loads a Cityscapes dataset using the dataset root, split, image directory, label directory, image suffix, label suffix, and train-ID option from the sidebar.</p> <p>It then displays samples and overlays the ground-truth segmentation mask on the image. This helped check that the GUI can read the same Cityscapes structure used in the tutorial.</p> <p>I also added a shared image grid utility for the GUI. The image detection viewer already had a paginated grid with search, and the image segmentation viewer needed the same behavior. Instead of duplicating it, I moved the grid code into a shared utility.</p> <p>This shared grid handles:</p> <ul> <li>pagination;</li> <li>search by sample name;</li> <li>image thumbnail selection;</li> <li>Streamlit session-state keys for each task.</li> </ul> <p>The detection and segmentation viewers can now reuse the same grid while keeping their task-specific rendering separate.</p> <h2 id="challenges">Challenges</h2> <p>The main challenge during these weeks was avoiding unnecessary duplication without over-abstracting too early. The GUI needs shared pieces, such as the image grid, but the actual task behavior should stay separate. Image detection, image segmentation, and LiDAR segmentation should not be forced into one generic viewer because their data and visualizations are different.</p> <p>Another challenge was understanding where ontology translation should happen. Exporting datasets to a new ontology is still useful, but evaluation can also use the translation directly when converted labels do not need to be stored.</p> <h2 id="next-steps">Next Steps</h2> <p>The next step is to continue the image segmentation GUI work by implementing the inference tab and evaluator tab for Cityscapes. This should follow the tutorial pattern by using a <code class="language-plaintext highlighter-rouge">TorchImageSegmentationModel</code>, a model configuration file, and an ontology file.</p> <p>After that, I plan to add image segmentation evaluation support in the GUI. Once the Cityscapes flow is working end to end, the same structure can be extended to other image segmentation datasets and later to LiDAR segmentation. I will also be exploring how to view open3d point clouds in the GUI for LiDAR segmentation.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="gui"/><category term="segmentation"/><category term="cityscapes"/><category term="nuimages"/><category term="semantickitti"/><summary type="html"><![CDATA[Improving segmentation evaluation with ontology translation and starting the PerceptionMetrics GUI task refactor]]></summary></entry><entry><title type="html">Coding Period Week 3</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-3/" rel="alternate" type="text/html" title="Coding Period Week 3"/><published>2026-06-14T00:00:00+00:00</published><updated>2026-06-14T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-3</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-3/"><![CDATA[<p>The third week of the coding period focused on improving the PerceptionMetrics documentation and preparing a nuImages image segmentation tutorial. After working on SemanticKITTI support and the LiDAR segmentation tutorial last week, I shifted focus to making the existing PerceptionMetrics workflows easier to understand and documenting the steps needed to evaluate an image segmentation model on nuImages.</p> <h2 id="pull-requests">Pull Requests</h2> <p>The main pull requests related to this week’s work are:</p> <ul> <li>nuImages tutorial: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/599">JdeRobot/PerceptionMetrics#599</a></li> <li>Documentation tabs: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/602">JdeRobot/PerceptionMetrics#602</a> <h2 id="documentation-improvements">Documentation Improvements</h2> </li> </ul> <p>This week, I reviewed the existing PerceptionMetrics documentation to understand what was already available and what still needed to be explained more clearly. The project already has structured documentation and autogenerated Python API pages.</p> <p>After discussing the documentation structure with the mentors, we agreed that separate documentation tabs for datasets, models, metrics, and other major workflows would make the documentation easier to navigate. Based on that, I worked on adding more human-readable documentation pages for the main parts of the library. This included documentation for datasets, models, and metrics. The goal was to explain what is currently supported in the codebase, how the components are connected.</p> <p>For the dataset documentation, I focused on describing the available dataset adapters and how they fit into the PerceptionMetrics evaluation workflow. I also checked which datasets are already connected to the CLI and which ones still need clearer CLI support or documentation.</p> <p>For the model documentation, I documented the available model wrappers and clarified how model registration works in PerceptionMetrics.</p> <p>I also added a metrics documentation page. This explains the segmentation metrics reported by PerceptionMetrics, including precision, recall, accuracy, F1-score, IoU, and Dice score.</p> <h2 id="nuimages-segmentation-tutorial">nuImages Segmentation Tutorial</h2> <p>The second major task this week was preparing the nuImages image segmentation tutorial. As suggested by David, I fine-tuned a SegFormer model on the nuImages dataset and uploaded the trained checkpoint to <a href="https://huggingface.co/tejasstanley/segformer-nuimages-b0-full-bs8/tree/main">Hugging Face</a>. I then used this model in the tutorial and connected it to the PerceptionMetrics image segmentation evaluation workflow.</p> <p>The tutorial covers loading the nuImages dataset through the PerceptionMetrics adapter, preparing the model configuration, loading the segmentation model, running predictions, and evaluating the results.</p> <p>I created a model ontology that matches the labels predicted by the trained model. I also created an evaluation ontology with an <code class="language-plaintext highlighter-rouge">ignore</code> class so that classes not predicted by the model, such as <code class="language-plaintext highlighter-rouge">background</code> and <code class="language-plaintext highlighter-rouge">vehicle.ego</code>, can be handled properly during evaluation.</p> <p>The tutorial exports the dataset labels into the evaluation ontology before running metrics. I added prediction inspection steps to the notebook. These print the full set of classes predicted by the model and the classes expected by the dataset, which helps debug ontology mismatches. I added visualization of the input image, prediction, and ground-truth label side by side as expected in a typical image segmentation tutorial.</p> <p>The model initialization and dataset initialization are now separated into different cells, and I added markdown explanations around the ontology export, model wrapper setup, prediction inspection, and evaluation steps.</p> <p>Alongside this, David reviewed and merged my commit for Cityscapes. This helped close one more part of the image segmentation dataset work and gave me a clearer reference for how the nuImages workflow should fit with the rest of the project.</p> <h2 id="challenges">Challenges</h2> <p>The main challenge this week was deciding how much detail should go into documentation pages and how much should stay in tutorials. The documentation pages should explain the supported components and expected formats, while tutorials should show a complete workflow with real examples.</p> <p>Another challenge was handling the ontology mismatch in the nuImages tutorial. The model and dataset do not use exactly the same label space, so I had to carefully set up the model ontology, evaluation ontology, ignored classes, and translation files.</p> <h2 id="next-steps">Next Steps</h2> <p>Next week, as suggested by David, I plan to look into the evaluation function and explore how it can work directly with ontology translation without requiring a separate dataset export step. This would make the evaluation workflow cleaner, especially for datasets where the model and dataset ontologies do not match exactly.</p> <p>Another goal for next week is to explore GUI support for LiDAR segmentation. I plan to look for bugs in the current GUI workflow and take support from Praveena to refine the implementation and develop support for LiDAR segmentation evaluation.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="documentation"/><category term="nuimages"/><category term="segmentation"/><summary type="html"><![CDATA[Improving PerceptionMetrics documentation and preparing the nuImages image segmentation tutorial]]></summary></entry><entry><title type="html">Coding Period Week 2</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-2/" rel="alternate" type="text/html" title="Coding Period Week 2"/><published>2026-06-06T00:00:00+00:00</published><updated>2026-06-06T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-2</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-2/"><![CDATA[<p>The second week of the coding period focused on adding SemanticKITTI support to PerceptionMetrics and preparing a tutorial around it. After studying the dataset structure last week, I started implementing a dataset adapter and building a workflow for loading LiDAR point clouds, reading semantic labels, running model inference through MMDetection3D, and using the results with PerceptionMetrics.</p> <h2 id="semantickitti-dataset-adapter">SemanticKITTI Dataset Adapter</h2> <p>This week, I worked on the SemanticKITTI dataset adapter, which is tracked in <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/588">JdeRobot/PerceptionMetrics#588</a>. Each SemanticKITTI sample contains a <code class="language-plaintext highlighter-rouge">.bin</code> point cloud file and a corresponding <code class="language-plaintext highlighter-rouge">.label</code> file, organized by sequence.</p> <p>The goal was to make the adapter follow the existing PerceptionMetrics dataset structure while correctly handling the SemanticKITTI format. I focused on loading point cloud files, matching them with their label files, and preparing dataset entries that can be used by the evaluation pipeline.</p> <p>I also worked on the SemanticKITTI ontology. This is needed because the raw dataset labels must be mapped to semantic classes before evaluation. For LiDAR semantic segmentation, this step is especially important because labels are stored in an encoded format and need to be decoded before they can be compared with predictions.</p> <h2 id="semantickitti-tutorial">SemanticKITTI Tutorial</h2> <p>Alongside the adapter, I started preparing the SemanticKITTI tutorial, which is tracked in <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/590">JdeRobot/PerceptionMetrics#590</a>. The tutorial explains how to set up the dataset, load samples through the adapter, inspect the ontology, run inference, and prepare the data for evaluation.</p> <p>It also documents the dataset structure and explains how point clouds and labels are paired. This is useful because SemanticKITTI is less straightforward than many image segmentation datasets, especially for users who are new to LiDAR semantic segmentation.</p> <p>Writing the tutorial while developing the adapter helped test the workflow from a user perspective. Parts that were unclear in the tutorial often pointed to areas where the adapter or documentation needed improvement.</p> <h2 id="using-mmdetection3d">Using MMDetection3D</h2> <p>For the tutorial, I used MMDetection3D as the model-side framework for SemanticKITTI. MMDetection3D provides existing support for 3D perception models, pretrained configurations, and LiDAR semantic segmentation workflows. This allowed the tutorial to go beyond dataset loading and show how the adapter can be used in a practical model inference workflow.</p> <p>I explored how to load a SemanticKITTI-compatible model from MMDetection3D, run inference on point cloud samples, and connect the predicted semantic labels back to the PerceptionMetrics workflow. In this setup, the adapter provides the dataset structure and ontology, while MMDetection3D provides model predictions that can later be evaluated using PerceptionMetrics.</p> <p>This also helped identify the information that needs to stay aligned between the two libraries: point cloud data, predicted labels, ground-truth labels, and class mappings.</p> <p>The tutorial now shows a complete workflow where users can load data with the PerceptionMetrics adapter, use MMDetection3D for inference, and prepare the outputs for evaluation.</p> <h2 id="challenges">Challenges</h2> <p>The main challenge this week was correctly handling the SemanticKITTI format. The dataset uses sequence-based folders, binary point cloud files, and encoded label files, so the adapter needs to handle file matching and label decoding carefully.</p> <p>Another challenge was deciding what should be part of the adapter and what should remain in the tutorial or future model integration work. For now, I kept the adapter focused on loading and organizing the data, while the tutorial explains how to use it with MMDetection3D and PerceptionMetrics.</p> <h2 id="next-steps">Next Steps</h2> <p>Next week, I plan to focus on documentation improvements. This includes refining the documentation for existing adapters, models, functionalities and evaluation workflows existing in PerceptionMetrics. Clear documentation is crucial for making the library accessible to new users.</p> <p>I also plan to start exploring nuImages model training. This will help prepare for the next stage of dataset and model integration work, especially for understanding which pretrained or trainable models can be used with nuImages and how they can fit into the PerceptionMetrics evaluation pipeline.</p> <p>Another goal is to check the feasibility of using Vispy for point cloud visualization in the tutorial. This will be important for showing users how to visualize SemanticKITTI point clouds and predicted labels effectively. Comparing Vispy with Open3D will help determine the best approach for visualization in the tutorial.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="semantickitti"/><category term="lidar"/><summary type="html"><![CDATA[Implementing the SemanticKITTI dataset adapter and preparing the tutorial with MMDetection3D and PerceptionMetrics]]></summary></entry><entry><title type="html">Coding Period Week 1</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-1/" rel="alternate" type="text/html" title="Coding Period Week 1"/><published>2026-05-29T00:00:00+00:00</published><updated>2026-05-29T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-1</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-1/"><![CDATA[<p>The first week of the coding period was focused on testing the Cityscapes dataset adapter I started during the community bonding period, improving its implementation based on feedback from my mentors, preparing the tutorial for Cityscapes image segmentation, and exploring the SemanticKITTI dataset to understand how to support it in PerceptionMetrics.</p> <h2 id="improving-the-cityscapes-adapter">Improving the Cityscapes Adapter</h2> <p>This week, I continued working on the Cityscapes dataset adapter and updated it based on the feedback and merges from last week. The main goal was to make the adapter fit better with the existing PerceptionMetrics dataset interface and ensure that it can be used cleanly from the evaluation pipeline.</p> <p>I worked on cleaning up the adapter implementation, registering the dataset so that it can be built through the common dataset creation workflow, and fixing the arguments passed through <code class="language-plaintext highlighter-rouge">build_dataset</code>. I also restored the required <code class="language-plaintext highlighter-rouge">cityscapesscripts</code> dependency, since the adapter uses the official Cityscapes label definitions to build the ontology and class mappings.</p> <p>Another important part of the work was checking how the adapter handles Cityscapes train IDs and label IDs. This is important because many pretrained segmentation models use train IDs, while the original dataset also provides raw label IDs. Making this configurable should make the adapter easier to use with different evaluation setups.</p> <h2 id="cityscapes-tutorial">Cityscapes Tutorial</h2> <p>I also completed preparing a dedicated Cityscapes image segmentation tutorial for PerceptionMetrics. The tutorial shows how to load the Cityscapes dataset, configure the ontology, use a pretrained segmentation model, and export the data in a format that can be evaluated by the existing metrics pipeline.</p> <p>For this, I experimented with a pretrained SegFormer model fine-tuned on Cityscapes and checked how its predictions align with the ontology generated by the adapter. This helped me verify the adapter beyond just loading files, because the tutorial exercises the complete flow from dataset loading to prediction and evaluation preparation.</p> <p>A screenshot of the tutorial is shown below, where you can see the predicted segmentation masks overlaid on the original Cityscapes images.</p> <p><img src="/gsoc2026-Tejas_Stanley/assets/img/week1_city_tut.png" alt="Cityscapes Tutorial" style="max-width: 100%; margin-bottom: 1.5rem;"/></p> <h2 id="exploring-semantickitti">Exploring SemanticKITTI</h2> <p>In parallel, I started exploring the SemanticKITTI dataset to understand what will be needed for future support. Since SemanticKITTI is a LiDAR semantic segmentation dataset, it has a different structure from Cityscapes and requires working with point clouds, binary label files, and sequence-based organization.</p> <p>I looked at the existing LiDAR utilities in PerceptionMetrics and compared them with the SemanticKITTI data format. This helped me identify which parts of the current codebase can already be reused and which parts will need a new dataset adapter or additional configuration.</p> <p>David also suggested using the MMDetection3D codebase as the model zoo for SemanticKITTI, since it has many pretrained models and a well-established evaluation pipeline. The adapter will follow a structure similar to the usual LiDAR dataset adapters. This will involve loading the point cloud data, matching it with the corresponding label files, and building an ontology based on the SemanticKITTI class definitions.</p> <p>The visualization of point clouds and predicted labels using VisPy looked very good, and David suggested comparing both Open3D and VisPy for this part. This will be important for the tutorial, since visualizing point clouds with predicted labels is a key part of understanding the results. David also suggested experimenting with the LiDAR utilities available in <code class="language-plaintext highlighter-rouge">torch_segmentation.py</code>.</p> <p>Below is a screenshot of the beautiful visualizations of SemanticKITTI point clouds and labels using VisPy, which will be a great reference for the tutorial.</p> <p><img src="/gsoc2026-Tejas_Stanley/assets/img/week1_sem_kitti_vis.png" alt="SemanticKITTI Visualization" style="max-width: 100%; margin-bottom: 1.5rem;"/></p> <h2 id="next-steps">Next Steps</h2> <p>This week, David merged both the Cityscapes adapter and the tutorial, which is great progress. In the coming week, I plan to start working on the SemanticKITTI dataset adapter, focusing on loading point cloud data and matching it with the corresponding labels.</p> <p>I also plan to experiment more with the existing LiDAR utilities, compare Open3D and VisPy for visualization, and start preparing the structure for a future SemanticKITTI tutorial.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="cityscapes"/><summary type="html"><![CDATA[Implementing the Cityscapes tutorial and improving the dataset adapter, exploring SemanticKITTI dataset]]></summary></entry><entry><title type="html">Community Bonding Week 1</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/community-bonding-week-1/" rel="alternate" type="text/html" title="Community Bonding Week 1"/><published>2026-05-20T00:00:00+00:00</published><updated>2026-05-20T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/community-bonding-week-1</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/community-bonding-week-1/"><![CDATA[<p>The second week of the GSoC community bonding period was focused on diving deeper into the PerceptionMetrics codebase, understanding how dataset adapters are implemented, and beginning to experiment with evaluation workflows. My main goal for the week was to understand the existing dataset structure in the project and start preparing the initial support for Cityscapes.</p> <h2 id="exploring-perceptionmetrics">Exploring PerceptionMetrics</h2> <p>I spent time going through the evaluation pipeline in PerceptionMetrics to understand how datasets, ontologies, predictions, and labels are connected during evaluation. This helped me understand what information a dataset adapter needs to provide and how the data should be formatted before it can be used by the metrics module.</p> <p>I also looked at the existing GOOSE dataset adapter as a reference. This was useful for understanding the expected structure of dataset dictionaries, ontology dictionaries, and how image-label pairs are collected before being passed to the segmentation dataset wrapper.</p> <h2 id="starting-the-cityscapes-adapter">Starting the Cityscapes Adapter</h2> <p>I started integrating support for the Cityscapes dataset by writing an initial adapter for semantic segmentation. The adapter loads Cityscapes images and matches them with their corresponding ground-truth label files based on the standard Cityscapes folder structure.</p> <p>This involved handling the train, validation, and test splits, building image and label paths, and creating an ontology from the official Cityscapes label definitions. I also worked on making the implementation configurable so that different image and label directories or suffixes can be passed if needed.</p> <h2 id="community-meeting">Community Meeting</h2> <p>This week, I also attended a group meeting with the JdeRobot team. During the meeting, we introduced ourselves, discussed the project methodology, and reviewed the expected timelines and outcomes for GSoC.</p> <p>It was helpful to hear about the broader goals of the organization.</p> <h2 id="next-steps">Next Steps</h2> <p>In the coming week, I plan to continue improving the Cityscapes dataset adapter and test it more thoroughly with the PerceptionMetrics evaluation pipeline. I will also refine the implementation based on feedback from my mentors, clean up the API where needed, and start checking how the adapter behaves with real Cityscapes samples.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="cityscapes"/><summary type="html"><![CDATA[Exploring PerceptionMetrics, Cityscapes, and evaluation workflows]]></summary></entry><entry><title type="html">Community Bonding Week 0</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/community-bonding-week-0/" rel="alternate" type="text/html" title="Community Bonding Week 0"/><published>2026-05-14T00:00:00+00:00</published><updated>2026-05-14T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/community-bonding-week-0</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/community-bonding-week-0/"><![CDATA[<p>The first week of the GSoC community bonding period has mainly been focused on getting everything set up and becoming more familiar with the project workflow and datasets.</p> <h2 id="meeting-the-mentors">Meeting the Mentors</h2> <p>This week, I had discussions with the mentors regarding the overall direction of the project, expectations for the coding period, and the planned milestones for the upcoming weeks.</p> <h2 id="setting-up-the-blog">Setting Up the Blog</h2> <p>I also spent some time setting up this blog using the al-folio Jekyll theme and GitHub Pages. Since this is my first time maintaining a project blog in this format, there was a bit of debugging involved with GitHub Actions, deployment configuration, and asset management.</p> <p>The website is now live and will be used to document weekly progress throughout Google Summer of Code 2026.</p> <h2 id="exploring-the-cityscapes-dataset">Exploring the Cityscapes Dataset</h2> <p>Another major part of this week involved exploring the Cityscapes dataset and understanding how semantic segmentation annotations are organized.</p> <p>I spent some time understanding:</p> <ul> <li>Dataset directory structure</li> <li>labelIds and trainIds</li> <li>Semantic segmentation masks</li> <li>Visualization pipelines</li> <li>Mapping RGB images to corresponding annotations</li> </ul> <p>I also began writing utilities for loading images and their corresponding segmentation labels, which will later help during integration into PerceptionMetrics.</p> <h2 id="next-steps">Next Steps</h2> <p>For the next week, the plan is to:</p> <ul> <li>Continue understanding the PerceptionMetrics codebase</li> <li>Explore dataset adapter implementations</li> <li>Begin integrating additional dataset support</li> <li>Start experimenting with evaluation workflows and metrics</li> </ul>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="cityscapes"/><summary type="html"><![CDATA[Setting up the blog, meeting mentors, and exploring Cityscapes]]></summary></entry></feed>