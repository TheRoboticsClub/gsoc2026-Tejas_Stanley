<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/feed.xml" rel="self" type="application/atom+xml"/><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-21T11:55:20+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/feed.xml</id><title type="html">Tejas Stanley: GSOC X JDE Robotics 2026</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Coding Period Week 12</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-12/" rel="alternate" type="text/html" title="Coding Period Week 12"/><published>2026-08-21T00:00:00+00:00</published><updated>2026-08-21T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-12</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-12/"><![CDATA[<p>This week I focused on finishing the remaining work around the GUI updates and documentation. I also spent some time exploring LiDAR detection, but this is additional work outside the original GSoC proposal, so I plan to continue it after Google Summer of Code ends.</p> <h2 id="gui-tests">GUI Tests</h2> <p>One of the main tasks this week was completing tests for the Streamlit GUI. I worked on GUI tests using Streamlit’s <code class="language-plaintext highlighter-rouge">AppTest</code>, so the app can be checked without manually opening the browser each time.</p> <p>The tests cover the main GUI flow for the supported tasks. This includes checking that the sidebar options are rendered correctly, dataset inputs can be updated, and the dataset viewer and evaluator tabs respond correctly when a small test dataset is loaded.</p> <p>This should help make future GUI changes safer, because regressions in the task selection, sidebar inputs, dataset viewer, or evaluator page can be caught earlier.</p> <h2 id="documentation-pr">Documentation PR</h2> <p>I also completed the documentation updates for the GUI. The GUI documentation was updated to match the newer task-based structure, since the GUI now supports more than only image detection.</p> <p>The documentation now includes the supported GUI tasks and tutorial videos for:</p> <ul> <li>image detection;</li> <li>image segmentation;</li> <li>LiDAR segmentation.</li> </ul> <p>I also worked on cleaning up the dataset documentation so that the supported datasets and their links are easier to find.</p> <h2 id="lidar-detection-exploration">LiDAR Detection Exploration</h2> <p>I continued exploring LiDAR detection a little more this week. The work was mainly about understanding how it could fit into the existing PerceptionMetrics structure, especially after the earlier nuScenes and MMDetection3D experiments.</p> <p>This work is still exploratory. Since LiDAR detection was not part of the original GSoC proposal, I do not want to rush it into the final GSoC scope. The plan is to keep it as follow-up work and complete it properly after the program ends.</p> <h2 id="next-steps">Next Steps</h2> <p>The main remaining work is to get the documentation and GUI test PRs reviewed and merged. After that, I will focus on wrapping up the final GSoC submission and keeping the LiDAR detection work separate as future development.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="gui"/><category term="tests"/><category term="documentation"/><category term="lidar"/><category term="detection"/><summary type="html"><![CDATA[Final GUI tests, documentation updates, and LiDAR detection exploration]]></summary></entry><entry><title type="html">Final Report</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/final-report/" rel="alternate" type="text/html" title="Final Report"/><published>2026-08-21T00:00:00+00:00</published><updated>2026-08-21T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/final-report</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/final-report/"><![CDATA[<h1 id="perceptionmetrics-dataset-support-gui-extensions-tutorials-and-tests">PerceptionMetrics: Dataset Support, GUI Extensions, Tutorials, and Tests</h1> <hr/> <p>Organization: <a href="https://jderobot.github.io/">JdeRobot</a></p> <p>Contributor: Tejas Stephen Stanley</p> <p>Project: PerceptionMetrics dataset support, GUI extensions, tutorials, and tests</p> <hr/> <p>Hello everyone,</p> <p>This report summarizes my Google Summer of Code 2026 work with JdeRobot on <a href="https://jderobot.github.io/PerceptionMetrics/">PerceptionMetrics</a>. Over the summer, I worked on improving dataset support, extending the GUI for image and LiDAR segmentation, preparing tutorials and documentation, and adding tests to make the project more robust.</p> <hr/> <h2 id="about-me">About Me</h2> <p>I am Tejas Stephen Stanley, a Master’s student in Robotics at TU Delft. This summer, I had the opportunity to contribute to JdeRobot through Google Summer of Code 2026.</p> <p>Before starting the project, I had worked with robotics and perception systems, but this project helped me understand model evaluation workflows in much more depth. Working on PerceptionMetrics gave me experience with dataset adapters, ontology handling, model evaluation, GUI development, documentation, and testing in an open-source project.</p> <hr/> <h2 id="project-summary">Project Summary</h2> <p><a href="https://jderobot.github.io/PerceptionMetrics/">PerceptionMetrics</a> is a toolkit for evaluating perception models across frameworks and datasets. Past GSoC projects (<a href="https://github.com/TheRoboticsClub/2017-colab-vinay_sharma">Vinay Sharma, 2017</a>, <a href="https://theroboticsclub.github.io/colab-gsoc2019-Jeevan_Kumar/">Jeevan Kumar, 2019</a>) contributed to its first stable release, published in Sensors (<a href="https://doi.org/10.3390/s22124575">Paniego et al., 2022</a>). Recently, the tool has been revamped to support LiDAR, image segmentation, and object detection (<a href="https://github.com/TheRoboticsClub/gsoc2025-Sakhineti_Praveena">Sakhineti Praveena, 2025</a>).</p> <p>My project focused on continuing this work by adding support for more perception datasets, improving the GUI so that it can handle more tasks, and creating tutorials and tests that make the project easier to use and maintain.</p> <p>The main goals were:</p> <ul> <li>Exploring core segmentation and detection datasets used in industry and research for image and LiDAR perception, such as Cityscapes, nuImages, and SemanticKITTI, then prioritizing and adding support for them.</li> <li>Extending the GUI to support image and LiDAR segmentation visualization, since the GUI was previously focused mainly on object detection.</li> <li>Generating tutorials and documentation for different use cases, models, and data formats.</li> <li>Improving the robustness of the project through a better test suite.</li> </ul> <hr/> <figure> <video src="/gsoc2026-Tejas_Stanley/assets/video/perception_metrics_final_ppt.mp4" width="100%" height="auto" controls=""/> </figure> <hr/> <h2 id="contribution-summary">Contribution Summary</h2> <h3 id="1-dataset-support">1. Dataset Support</h3> <p>The first major part of the project was working on dataset adapters. I started with Cityscapes and then moved to SemanticKITTI.</p> <p>For Cityscapes, I worked on the image segmentation adapter. This helped set up the pattern for evaluating image segmentation datasets through PerceptionMetrics.</p> <p>For SemanticKITTI, I added support for LiDAR semantic segmentation. This included loading point cloud <code class="language-plaintext highlighter-rouge">.bin</code> files, matching them with <code class="language-plaintext highlighter-rouge">.label</code> files, reading the SemanticKITTI ontology, and preparing the dataset for evaluation.</p> <p>The main dataset-related pull requests were:</p> <ul> <li>Cityscapes dataset adapter: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/577">JdeRobot/PerceptionMetrics#577</a></li> <li>SemanticKITTI dataset adapter: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/588">JdeRobot/PerceptionMetrics#588</a></li> </ul> <h3 id="2-segmentation-tutorials-and-evaluation-workflow">2. Segmentation Tutorials and Evaluation Workflow</h3> <p>Another important part of the project was preparing tutorials for the supported datasets. The goal was not only to add dataset classes, but also to make the full workflow understandable for users.</p> <p>I worked on tutorials for:</p> <ul> <li><a href="https://github.com/JdeRobot/PerceptionMetrics/blob/master/examples/tutorial_cityscapes_image_segmentation.ipynb">Cityscapes image segmentation</a>;</li> <li><a href="https://github.com/JdeRobot/PerceptionMetrics/blob/master/examples/tutorial_nuimages_segmentation.ipynb">nuImages image segmentation</a>;</li> <li><a href="https://github.com/JdeRobot/PerceptionMetrics/blob/master/examples/tutorial_semanticKITTI_lidar_segmentation.ipynb">SemanticKITTI LiDAR segmentation</a>.</li> </ul> <p>While working on the tutorials, I also explored how evaluation should work when the model labels and dataset labels do not directly match. Earlier workflows often required exporting the dataset into the model ontology first. During this project, I worked with ontology translation files so that evaluation can be done more directly.</p> <p>This helped make the tutorials cleaner and reduced the need for extra dataset export steps in cases where label IDs differ.</p> <p>Related pull requests:</p> <ul> <li>Cityscapes tutorial: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/580">JdeRobot/PerceptionMetrics#580</a></li> <li>SemanticKITTI tutorial: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/590">JdeRobot/PerceptionMetrics#590</a></li> <li>nuImages segmentation tutorial: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/599">JdeRobot/PerceptionMetrics#599</a></li> <li>Segmentation evaluation and tutorial updates: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/605">JdeRobot/PerceptionMetrics#605</a></li> </ul> <h3 id="3-gui-extensions">3. GUI Extensions</h3> <p>The GUI was another major part of the project. At the start, the GUI was mainly structured around image object detection. During the project, I worked on extending this structure so that image segmentation and LiDAR segmentation could also fit into it.</p> <p>For image segmentation, I worked on:</p> <ul> <li>task-specific sidebar inputs;</li> <li>Cityscapes and nuImages dataset viewer support;</li> <li>image grid selection;</li> <li>mask overlay visualization;</li> <li>model loading for SegFormer-based workflows;</li> <li>inference visualization;</li> <li>evaluator tab support using PerceptionMetrics <code class="language-plaintext highlighter-rouge">model.eval()</code>.</li> </ul> <p>For LiDAR segmentation, I worked on:</p> <ul> <li>SemanticKITTI dataset loading in the GUI;</li> <li>point cloud visualization using Plotly;</li> <li>coloring points by semantic label or intensity;</li> <li>class color tables;</li> <li>frame selection;</li> <li>inference visualization;</li> <li>evaluator tab support;</li> <li>controls for point size, maximum points, and intensity clipping.</li> </ul> <p>The GUI work made the application more task-based. Instead of forcing all tasks through one object detection flow, each task can now have its own sidebar and tab logic while still sharing the same main Dataset Viewer, Inference, and Evaluator structure.</p> <p>Related pull request:</p> <ul> <li>Image segmentation GUI: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/609/">JdeRobot/PerceptionMetrics#609</a></li> <li>LiDAR segmentation GUI: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/618">JdeRobot/PerceptionMetrics#618</a></li> </ul> <h3 id="4-gui-tutorials-and-documentation">4. GUI Tutorials and Documentation</h3> <p>I also worked on documentation and tutorial material for the GUI. Since the GUI now supports multiple tasks, the documentation needed to be updated to explain the task-based workflow.</p> <p>I prepared video tutorials for:</p> <ul> <li>image detection GUI;</li> <li>image segmentation GUI;</li> <li>LiDAR segmentation GUI.</li> </ul> <p>The videos were prepared using Canva with text-to-speech narration, so that each GUI step can be followed clearly. I also updated the GUI documentation page to include the tutorial videos and updated the dataset documentation with supported datasets and dataset links.</p> <p>Related pull request:</p> <ul> <li>Documentation updates: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/602">JdeRobot/PerceptionMetrics#602</a></li> </ul> <h3 id="5-tests">5. Tests</h3> <p>In the final part of the project, I worked on improving the test suite.</p> <p>I added tests for dataset adapters such as:</p> <ul> <li>Cityscapes;</li> <li>nuImages;</li> <li>SemanticKITTI;</li> <li>nuScenes LiDAR detection exploration.</li> </ul> <p>I also worked on GUI tests using Streamlit’s <code class="language-plaintext highlighter-rouge">AppTest</code>. These tests check that the GUI renders correctly, sidebar controls update session state, tiny fake datasets can be loaded, and dataset viewer and evaluator tabs respond as expected.</p> <p>This work is important because the GUI now has more task-specific behavior. The tests make it easier to catch regressions when the GUI or dataset loading logic changes in the future.</p> <p>Related pull request:</p> <ul> <li>Dataset and GUI tests: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/627">JdeRobot/PerceptionMetrics#627</a></li> </ul> <hr/> <h2 id="on-github">On GitHub</h2> <p>Over the summer, I worked on several pull requests covering dataset adapters, tutorials, GUI support, documentation, and tests. The main pull requests are grouped below week by week. Some pull requests appear in more than one week because the same work continued across review, cleanup, and follow-up changes.</p> <table> <thead> <tr> <th>Week</th> <th>Pull Request</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td>Week 1</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/577">#577</a>, <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/580">#580</a></td> <td>Cityscapes image segmentation dataset adapter and tutorial notebook.</td> </tr> <tr> <td>Week 2</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/588">#588</a>, <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/590">#590</a></td> <td>SemanticKITTI LiDAR segmentation dataset adapter and tutorial notebook.</td> </tr> <tr> <td>Week 3</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/599">#599</a>, <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/602">#602</a></td> <td>nuImages image segmentation tutorial and documentation pages for datasets, models, and metrics.</td> </tr> <tr> <td>Weeks 4-5</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/599">#599</a>, <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/605">#605</a></td> <td>Continued nuImages tutorial updates and segmentation evaluation changes using ontology translation.</td> </tr> <tr> <td>Week 6</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/609">#609</a>, <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/618">#618</a></td> <td>Started image segmentation GUI support and SemanticKITTI point cloud viewer experiments.</td> </tr> <tr> <td>Week 7</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/618">#618</a></td> <td>Added LiDAR segmentation dataset viewer, inference, and evaluator work.</td> </tr> <tr> <td>Week 8</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/609">#609</a></td> <td>Cleaned up image segmentation GUI after review comments.</td> </tr> <tr> <td>Weeks 9-10</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/609">#609</a>, <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/618">#618</a></td> <td>Incorporated recent image detection GUI changes into the image segmentation and LiDAR segmentation GUI branches.</td> </tr> <tr> <td>Week 11</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/627">#627</a></td> <td>Dataset adapter tests, including Cityscapes, nuImages, SemanticKITTI, and nuScenes exploration.</td> </tr> <tr> <td>Week 12</td> <td><a href="https://github.com/JdeRobot/PerceptionMetrics/pull/602">#602</a>, <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/627">#627</a></td> <td>Final documentation updates, GUI videos, dataset links, support tables, and Streamlit GUI tests.</td> </tr> </tbody> </table> <hr/> <h2 id="future-work">Future Work</h2> <p>There are several directions in which this work can be continued:</p> <ol> <li> <p><strong>Complete LiDAR detection support</strong></p> <p>I started exploring LiDAR detection with KITTI, nuScenes, and MMDetection3D. This is useful future work, but it was outside the original GSoC proposal, so it should be completed separately after the program.</p> </li> <li> <p><strong>Improve model support in the GUI</strong></p> <p>The GUI can be extended to support more model formats and backends, especially for segmentation and LiDAR workflows.</p> </li> <li> <p><strong>Expand tests</strong></p> <p>The dataset and GUI tests can be extended further as more datasets and model types are added.</p> </li> </ol> <hr/> <h2 id="conclusion">Conclusion</h2> <p>Google Summer of Code 2026 with JdeRobot was a very valuable experience for me. I started by working on dataset adapters and gradually moved into tutorials, GUI development, testing, and documentation.</p> <p>This project helped me understand how perception evaluation tools are built across different datasets, model frameworks, and data formats. It also taught me how important good tutorials, documentation, and tests are for making an open-source project usable by others.</p> <p>I am especially thankful to my mentors, David Pascual and Sakhineti Praveena. They were very helpful throughout the project, reviewed my pull requests thoroughly, and helped me improve my workflow and understanding of testing, vision models, and evaluation pipelines. The project had a very positive environment, which made it easier to learn, ask questions, and keep improving the work.</p> <p>I am also grateful to the JdeRobot community for the opportunity and support. I plan to continue contributing to PerceptionMetrics after GSoC, especially around LiDAR detection and further GUI improvements.</p> <p>Thank you for following my GSoC journey.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="final-report"/><category term="datasets"/><category term="gui"/><category term="tutorials"/><category term="tests"/><summary type="html"><![CDATA[Final report for Google Summer of Code 2026 with JdeRobot and PerceptionMetrics]]></summary></entry><entry><title type="html">Coding Period Week 11</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-11/" rel="alternate" type="text/html" title="Coding Period Week 11"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-11</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-11/"><![CDATA[<p>This week I worked mainly on the initial setup for LiDAR detection support in PerceptionMetrics. I also worked on tests for the dataset adapters, so that the dataset loading logic can be checked without needing the full real datasets every time.</p> <h2 id="lidar-detection-setup">LiDAR Detection Setup</h2> <p>I started preparing the structure for LiDAR detection. The work was based on the exploration I had done earlier with KITTI and nuScenes 3D detection data.</p> <p>The main part of this was adding the initial skeleton for LiDAR detection in the PyTorch detection framework. This included a LiDAR detection torch dataset wrapper and a placeholder model class for future LiDAR detection models.</p> <p>The idea is similar to the existing image detection structure, but the data format is different. Image detection works with 2D boxes, while LiDAR detection needs 3D boxes. For this work, the 3D box format being used is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x, y, z, dx, dy, dz, yaw
</code></pre></div></div> <p>where <code class="language-plaintext highlighter-rouge">x, y, z</code> is the center of the box in the LiDAR frame, <code class="language-plaintext highlighter-rouge">dx, dy, dz</code> are the box dimensions, and <code class="language-plaintext highlighter-rouge">yaw</code> is the heading angle.</p> <p>I also added a placeholder interface for reading LiDAR point clouds in the detection dataset base class. This keeps the structure similar to LiDAR segmentation, where each dataset class is responsible for reading its own point cloud format.</p> <h2 id="nuscenes-lidar-detection-adapter">nuScenes LiDAR Detection Adapter</h2> <p>I also worked on a first nuScenes LiDAR detection dataset adapter.</p> <p>For nuScenes, the dataset uses tokens and JSON tables instead of simple annotation files. The adapter builds a dataset index from the <code class="language-plaintext highlighter-rouge">LIDAR_TOP</code> samples and stores:</p> <ul> <li>the point cloud file path;</li> <li>the nuScenes sample data token used to get annotations;</li> <li>the split name.</li> </ul> <p>One important point was that nuScenes annotations are stored in the global scene frame. For detection models and visualization, the boxes need to be in the LiDAR sensor frame. To handle this, I used the official nuScenes helper which returns boxes transformed into the selected sensor frame.</p> <p>The adapter currently reads the point cloud as <code class="language-plaintext highlighter-rouge">x, y, z, intensity</code>. The extra ring index stored by nuScenes is dropped so that the format stays consistent with the other LiDAR work.</p> <p>The labels are stored as numeric class IDs, while the class names remain available through the ontology. This follows the same pattern used by the existing image detection and segmentation datasets.</p> <h2 id="dataset-adapter-tests">Dataset Adapter Tests</h2> <p>I also worked on tests for four dataset adapters:</p> <ul> <li>Cityscapes;</li> <li>nuImages;</li> <li>SemanticKITTI;</li> <li>nuScenes LiDAR detection.</li> </ul> <p>The tests focus on the main dataset-building behavior instead of using the full datasets. For example, filesystem calls are patched so that the tests can simulate dataset files and labels without needing large real downloads.</p> <p>This helps check that:</p> <ul> <li>dataset rows are created correctly;</li> <li>sample names and split names are assigned correctly;</li> <li>labels or annotations are connected to the right samples;</li> <li>missing labels are handled properly;</li> <li>ontology information is created or loaded as expected.</li> </ul> <p>These tests should make it easier to catch regressions when dataset loading code is changed later.</p> <h2 id="next-steps">Next Steps</h2> <p>The LiDAR detection work is still at the setup stage. The next steps are to connect the skeleton to an actual LiDAR detection backend, most likely MMDetection3D, and then decide how the 3D detection metrics should be handled inside PerceptionMetrics.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="lidar"/><category term="detection"/><category term="datasets"/><category term="tests"/><summary type="html"><![CDATA[LiDAR detection setup and dataset adapter tests]]></summary></entry><entry><title type="html">Coding Period Week 9-10</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-9-10/" rel="alternate" type="text/html" title="Coding Period Week 9-10"/><published>2026-08-05T00:00:00+00:00</published><updated>2026-08-05T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-9-10</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-9-10/"><![CDATA[<p>During these two weeks, I worked on preparing the GUI tutorials, incorporating recent GUI review changes, and starting the LiDAR detection exploration. The main focus was to make the GUI easier to explain and test through short tutorial videos, and to understand how KITTI and nuScenes store 3D bounding boxes.</p> <h2 id="gui-tutorial-preparation">GUI Tutorial Preparation</h2> <p>One of the main tasks was preparing tutorial material for the GUI. I worked on three GUI tutorials and prepared them in Canva so that they can be used as short videos.</p> <p>The tutorials are focused on:</p> <ul> <li>image detection;</li> <li>image segmentation;</li> <li>LiDAR segmentation.</li> </ul> <p>I prepared the videos in Canva and used text-to-speech so that each step in the GUI can be explained clearly. This should make the tutorials easier to follow, especially for users who are opening the GUI for the first time.</p> <p>The tutorials cover the basic flow of selecting a task, configuring the dataset and model inputs from the sidebar, viewing dataset samples, running inference, and using the evaluator tab.</p> <h2 id="gui-review-changes">GUI Review Changes</h2> <p>Another part of the work was incorporating the recent image detection GUI changes into the image segmentation and LiDAR segmentation GUI branches.</p> <p>The image detection PR had updates in the task-based GUI structure, so I had to bring those changes into the other task implementations as well.</p> <p>I also worked on finalizing the image segmentation and LiDAR segmentation GUI pull requests for review. This mostly involved cleaning up the implementation, checking that the task-specific sidebars still worked correctly, and making sure the newer image detection changes did not break the segmentation GUI work.</p> <h2 id="lidar-detection-exploration">LiDAR Detection Exploration</h2> <p>I started exploring LiDAR detection as the next possible extension.</p> <p>For this, I created an exploratory notebook using the local KITTI dataset. The notebook reads the KITTI object detection data, including camera images, Velodyne point clouds, calibration files, and label files.</p> <p>The notebook includes:</p> <ul> <li>image visualization with 2D boxes;</li> <li>Velodyne point cloud visualization;</li> <li>projection of LiDAR points into the camera image;</li> <li>counting how many 3D boxes are present per frame;</li> <li>Open3D visualization of point clouds with 3D boxes;</li> <li>BEV plots for checking boxes from a top-down view.</li> </ul> <p>I also checked how MMDetection3D can be used for KITTI 3D detection. The PointPillars KITTI checkpoint predicts 3D boxes for <code class="language-plaintext highlighter-rouge">Pedestrian</code>, <code class="language-plaintext highlighter-rouge">Cyclist</code>, and <code class="language-plaintext highlighter-rouge">Car</code>. For nuScenes, I used the official nuScenes devkit because the dataset stores samples, annotations, sensors, calibration, and ego poses through JSON tables and tokens.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="gui"/><category term="segmentation"/><category term="lidar"/><category term="detection"/><category term="tutorials"/><summary type="html"><![CDATA[GUI tutorial preparation and LiDAR detection exploration]]></summary></entry><entry><title type="html">Coding Period Week 8</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-8/" rel="alternate" type="text/html" title="Coding Period Week 8"/><published>2026-07-17T00:00:00+00:00</published><updated>2026-07-17T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-8</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-8/"><![CDATA[<p>This week I continued working on the image segmentation part of the PerceptionMetrics GUI. The main focus was to clean up the implementation after review comments and make the dataset viewer, inference tab, and evaluator tab easier to maintain.</p> <p>The image segmentation GUI had already been started with Cityscapes and nuImages. This week was mostly about improving that work so that the structure is cleaner and can be extended to more datasets later.</p> <h2 id="pull-request">Pull Request</h2> <p>The main pull request related to this work is:</p> <ul> <li>Image segmentation GUI: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/609/">JdeRobot/PerceptionMetrics#609</a></li> </ul> <h2 id="dataset-viewer-cleanup">Dataset Viewer Cleanup</h2> <p>One of the main review comments was that the dataset viewer should not have a separate rendering function for every dataset. The loading logic can be different for Cityscapes and nuImages, but once the dataset is loaded, the viewer should render it in the same way.</p> <p>Earlier, the viewer had separate branches for Cityscapes and nuImages. I changed this so that the main viewer function loads the selected dataset and then passes it to a common display function.</p> <p>The common display function now handles:</p> <ul> <li>sample grid rendering;</li> <li>sample selection;</li> <li>mask opacity control;</li> <li>image and ground-truth overlay display;</li> <li>class table display.</li> </ul> <p>This keeps the dataset-specific part smaller. Cityscapes and nuImages still have their own dataset-loading details, but the actual viewer logic is shared.</p> <h2 id="dataset-loading">Dataset Loading</h2> <p>The dataset loading was also cleaned up. The GUI now has a common <code class="language-plaintext highlighter-rouge">load_image_segmentation_dataset</code> function, which selects the correct loader based on the dataset type.</p> <p>For Cityscapes, the GUI uses:</p> <ul> <li>dataset path;</li> <li>split;</li> <li>image directory;</li> <li>label directory;</li> <li>image suffix;</li> <li>label suffix;</li> <li>train-ID option.</li> </ul> <p>For nuImages, the GUI uses:</p> <ul> <li>dataset path;</li> <li>split;</li> <li>nuImages version;</li> <li>generated labels directory.</li> </ul> <p>The loaded datasets are cached in Streamlit session state using a dataset key. This avoids reloading the dataset every time Streamlit reruns the app, while still reloading correctly when the dataset path, split, or dataset-specific settings change.</p> <h2 id="cityscapes-label-handling">Cityscapes Label Handling</h2> <p>Another change was related to Cityscapes color labels. Earlier, the GUI had logic to manually convert RGB color masks into label IDs. This made the viewer more dataset-specific than it needed to be.</p> <p>The PerceptionMetrics image segmentation dataset base class already supports RGB labels through the <code class="language-plaintext highlighter-rouge">is_label_rgb</code> option. So I moved this responsibility back to the dataset class path. Now the GUI simply calls:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dataset</span><span class="p">.</span><span class="nf">read_label</span><span class="p">(</span><span class="n">label_fname</span><span class="p">)</span>
</code></pre></div></div> <p>This keeps the GUI simpler and makes the dataset class responsible for reading labels correctly.</p> <h2 id="inference-and-evaluation">Inference and Evaluation</h2> <p>The image segmentation inference tab lets the user upload an image and run prediction using the loaded segmentation model. The prediction is colorized using the model ontology and shown with an overlay, similar to the tutorial workflow.</p> <p>The evaluator tab uses the PerceptionMetrics <code class="language-plaintext highlighter-rouge">model.eval()</code> path. It supports ontology translation, which is needed when the model label IDs and dataset label IDs do not directly match.</p> <p>I also reused the dataset-loading logic from the dataset viewer in the evaluator to avoid duplicating the Cityscapes and nuImages dataset construction code.</p> <h2 id="next-steps">Next Steps</h2> <p>The next step is to finish addressing any remaining review comments for the image segmentation GUI PR and then extend the same structure to more image segmentation datasets. After this, the GUI documentation also needs to be updated because the GUI is now task-based rather than only image-detection focused.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="gui"/><category term="segmentation"/><category term="cityscapes"/><category term="nuimages"/><summary type="html"><![CDATA[Image segmentation GUI updates and review changes]]></summary></entry><entry><title type="html">Coding Period Week 7</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-7/" rel="alternate" type="text/html" title="Coding Period Week 7"/><published>2026-07-10T00:00:00+00:00</published><updated>2026-07-10T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-7</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-7/"><![CDATA[<p>This week, the main focus was to extend the GUI support for LiDAR segmentation using SemanticKITTI.</p> <p>In the previous week, I had started a first Plotly-based LiDAR visualization experiment. This week I moved the LiDAR part closer to a complete GUI flow with dataset viewing, inference, and evaluation.</p> <h2 id="lidar-segmentation-dataset-viewer">LiDAR Segmentation Dataset Viewer</h2> <p>The next main part of the week was the LiDAR segmentation GUI.</p> <p>The dataset viewer now uses the SemanticKITTI dataset structure and lets the user select a split and frame from the GUI. The selected frame is rendered as an interactive 3D point cloud using Plotly.</p> <p>I added support for two coloring modes:</p> <ul> <li>semantic label colors;</li> <li>intensity-based colors.</li> </ul> <p>The intensity values come from the <code class="language-plaintext highlighter-rouge">.bin</code> point cloud file itself. SemanticKITTI point clouds store each point as <code class="language-plaintext highlighter-rouge">x, y, z, intensity</code>, so intensity is an input feature from the LiDAR data, not a model output.</p> <p>When labels are available, the viewer can color points by class, A class table has been added to display the color mapping.</p> <p>For the Plotly view, I added a top-view camera option. This makes it easier to reset the visualization to a useful view when the user rotates or zooms around the point cloud. I also added hover text so the user can see point coordinates and class names when label information is available.</p> <h2 id="lidar-segmentation-inference">LiDAR Segmentation Inference</h2> <p>The model wiring follows the SemanticKITTI tutorial. The GUI loads the model using the required model configuration, checkpoint, ontology, and PerceptionMetrics model configuration. Once the model is loaded, the inference tab can run prediction on a selected <code class="language-plaintext highlighter-rouge">.bin</code> point cloud.</p> <p>The prediction is then visualized in the same Plotly point cloud viewer. This made it possible to compare the raw point cloud and the predicted semantic classes from inside the GUI.</p> <p>One thing I had to handle carefully was how Streamlit file uploads work. Uploaded files are stored in memory, while the PerceptionMetrics LiDAR model prediction path expects a local file path. Because of that, the uploaded <code class="language-plaintext highlighter-rouge">.bin</code> file is temporarily written to disk before calling the model prediction function.</p> <p><img src="/gsoc2026-Tejas_Stanley/assets/img/lidar_seg_infer.png" alt="LiDAR segmentation inference preview" style="max-width: 100%; margin-bottom: 1.5rem;"/></p> <h2 id="lidar-segmentation-evaluation">LiDAR Segmentation Evaluation</h2> <p>I also started wiring the evaluator tab for SemanticKITTI LiDAR segmentation.</p> <p>The evaluator loads the dataset, checks that the model is available, and calls the model <code class="language-plaintext highlighter-rouge">eval()</code> function from PerceptionMetrics.</p> <p>I added progress callbacks for LiDAR evaluation as well. This allows the GUI to show live progress while evaluation is running, instead of only showing results after the full dataset has finished.</p> <p>The evaluator also supports ontology translation, since SemanticKITTI labels, train IDs, and model outputs may not always directly match. This follows the same idea used in the segmentation tutorials.</p> <p><img src="/gsoc2026-Tejas_Stanley/assets/img/lidar_seg_eval.png" alt="LiDAR segmentation evaluation preview" style="max-width: 100%; margin-bottom: 1.5rem;"/></p> <h2 id="next-steps">Next Steps</h2> <p>The next step is to test the LiDAR segmentation GUI more carefully with SemanticKITTI and clean up the code before opening a separate pull request for it.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="gui"/><category term="segmentation"/><category term="semantickitti"/><category term="lidar"/><summary type="html"><![CDATA[Adding LiDAR segmentation GUI support with SemanticKITTI]]></summary></entry><entry><title type="html">Coding Period Week 6</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-6/" rel="alternate" type="text/html" title="Coding Period Week 6"/><published>2026-07-03T00:00:00+00:00</published><updated>2026-07-03T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-6</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-6/"><![CDATA[<p>This week I continued working on the PerceptionMetrics GUI. The main focus was to make the task-based GUI structure more useful for image segmentation, while also starting a first experiment for LiDAR segmentation visualization.</p> <p>In the previous weeks, the GUI was split into task folders so that image detection, image segmentation, and LiDAR segmentation could have their own sidebar, dataset viewer, inference tab, and evaluator tab. This week I worked more on filling in the image segmentation parts.</p> <h2 id="pull-requests">Pull Requests</h2> <p>The main pull request related to this week’s GUI work is:</p> <ul> <li>Image segmentation GUI: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/609/">JdeRobot/PerceptionMetrics#609</a></li> </ul> <h2 id="image-segmentation-gui">Image Segmentation GUI</h2> <p>I first continued with the image segmentation dataset viewer. Cityscapes and nuImages were the datasets I focused on. Other datasets can be added later by adding the required sidebar inputs and dataset viewer logic in the <code class="language-plaintext highlighter-rouge">sidebar.py</code> and <code class="language-plaintext highlighter-rouge">dataset_viewer.py</code> files.</p> <p>The viewer now uses the dataset path and dataset-specific inputs from the sidebar. For Cityscapes, this includes the image directory, label directory, image suffix, label suffix, and whether train IDs are being used. For nuImages, this includes the dataset version and the generated labels directory.</p> <p>The GUI shows the selected image along with the ground-truth mask overlay. This helped in checking whether the labels were being read correctly and whether the mask was aligned with the image.</p> <p>I also connected nuImages segmentation to the dataset viewer. For nuImages, the GUI uses the dataset version and the generated labels directory from the sidebar.</p> <h2 id="model-inputs-for-segmentation">Model Inputs for Segmentation</h2> <p>I worked on the model input part of the image segmentation sidebar. The model input can either be a model file path or a Hugging Face model folder. Since the current Cityscapes and nuImages examples use Hugging Face SegFormer models, I focused on making sure the GUI can handle those models correctly. The basic option for Torch model files is present, but the main tested flow for now is the Hugging Face model folder path. Support for other <code class="language-plaintext highlighter-rouge">.pt</code> or <code class="language-plaintext highlighter-rouge">.pth</code> model-loading cases can be extended with small changes in the model loading function.</p> <p>So the sidebar now asks for:</p> <ul> <li>model path or Hugging Face model folder;</li> <li>model config JSON;</li> <li>model ontology JSON.</li> </ul> <p>This follows the same idea as the tutorials, where the model is wrapped by the PerceptionMetrics model class using a config file and an ontology file.</p> <p>For Hugging Face SegFormer models, I checked how the downloaded model folder and config files should be passed.</p> <h2 id="inference-and-evaluation">Inference and Evaluation</h2> <p>I also worked on the image segmentation inference and evaluator tabs.</p> <p>For inference, the GUI lets the user upload an image and run prediction using the loaded segmentation model. The prediction is then visualized using the model ontology, similar to how the tutorial visualizes predictions. It is very similar to the inference tab of image detection.</p> <p>For evaluation, the GUI uses the same <code class="language-plaintext highlighter-rouge">model.eval()</code> path from PerceptionMetrics. Similar to the tutorials and the evaluation tab of image detection.</p> <p>I also added support for ontology translation in the evaluator. This is important for datasets like Cityscapes and nuImages, where the dataset labels and model output labels may not always use the same IDs.</p> <p>One issue that came up while testing was Streamlit session state. I tried simplifying some browse buttons by removing small callback functions, but Streamlit does not allow changing a widget key after the widget has already been created in the same run. Because of that, browse buttons that update text input fields still need callback functions.</p> <h2 id="lidar-segmentation-viewer-experiment">LiDAR Segmentation Viewer Experiment</h2> <p>As suggested by David and Praveena in the previous week, I also started a small experiment for LiDAR segmentation visualization using SemanticKITTI.</p> <p>The viewer scans the SemanticKITTI folder structure, lists available frames by split, and renders the selected point cloud. It can also use the SemanticKITTI YAML config to color points by labels when label files are available.</p> <p>I first wanted to try Open3D, but for embedding inside Streamlit, I used a Plotly-based 3D view for now. This gives an interactive point cloud directly in the browser and is easier to test inside the current GUI.</p> <p>The Plotly-based viewer supports panning, rotation, and zooming. It also shows the point coordinates when hovering over the point cloud. This makes it useful for quickly checking whether a SemanticKITTI frame is being loaded and rendered correctly.</p> <p>One issue I faced was that the Plotly viewer needs WebGL support in the browser. On my system, I had to enable <code class="language-plaintext highlighter-rouge">Override software rendering list</code> in Chrome settings before the 3D point cloud rendered properly.</p> <p>As discussed with David and Praveena, the visualization can also be extended later to provide rendered images from fixed views, such as top view and front view. These views could be useful for evaluation and visualization inside the GUI.</p> <p>The maximum number of rendered points can also be controlled from the sidebar. I kept the default at 50K points. I tried 100K points as well, but the browser started to lag, so 50K is a better default for now. This limit can be increased later if needed.</p> <p><img src="/gsoc2026-Tejas_Stanley/assets/img/LIDAR_plotly_render.png" alt="LiDAR segmentation dataset viewer preview" style="max-width: 100%; margin-bottom: 1.5rem;"/></p> <h2 id="next-steps">Next Steps</h2> <p>I will be completing the LiDAR segmentation viewer and adding inference and evaluation support for SemanticKITTI.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="gui"/><category term="segmentation"/><category term="cityscapes"/><category term="nuimages"/><category term="semantickitti"/><category term="lidar"/><summary type="html"><![CDATA[Extending the PerceptionMetrics GUI for image segmentation and starting LiDAR visualization]]></summary></entry><entry><title type="html">Coding Period Weeks 4 and 5</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-4-5/" rel="alternate" type="text/html" title="Coding Period Weeks 4 and 5"/><published>2026-06-26T00:00:00+00:00</published><updated>2026-06-26T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-4-5</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-4-5/"><![CDATA[<p>During weeks 4 and 5 of the coding period, I worked on two main things in PerceptionMetrics. First, I looked at how segmentation evaluation can work when the model labels and dataset labels do not directly match. Second, I started refactoring the GUI so image detection, image segmentation, and LiDAR segmentation can be handled as separate tasks.</p> <p>This work came after the nuImages segmentation tutorial and the SemanticKITTI tutorial from the previous weeks.</p> <p>I also had to take a one-week leave of absence during this period to complete my exams. I informed my mentors about this in advance, and since the project progress was already ahead of the expected pace, we agreed that this would be manageable without affecting the overall timeline.</p> <h2 id="pull-requests">Pull Requests</h2> <p>The main pull requests related to the tutorial updates are:</p> <ul> <li>nuImages segmentation tutorial: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/599">JdeRobot/PerceptionMetrics#599</a></li> <li>Segmentation evaluation and tutorial updates: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/605">JdeRobot/PerceptionMetrics#605</a></li> </ul> <h2 id="evaluation-and-ontology-translation">Evaluation and Ontology Translation</h2> <p>I looked at the <code class="language-plaintext highlighter-rouge">eval</code> function for segmentation models and how it handles different label spaces. Earlier tutorials used the dataset <code class="language-plaintext highlighter-rouge">export</code> function to store labels in a different ontology before evaluation. This works, but evaluation should not always need a dataset export just because the model and dataset use different label IDs.</p> <p>While checking this, I found that ontology translation can help avoid this extra export step. Instead of saving another version of the dataset, evaluation can use a translation ontology or lookup table directly.</p> <p>This is useful for datasets such as:</p> <ul> <li>nuImages image segmentation</li> <li>Cityscapes image segmentation</li> <li>SemanticKITTI LiDAR segmentation</li> </ul> <p>In these datasets, the raw dataset labels, train IDs, model output IDs, and ignored classes may differ. The main point is that ontology translation can be used during evaluation, while dataset export is still useful when converted labels need to be saved on disk.</p> <h2 id="starting-the-gui-refactor">Starting the GUI Refactor</h2> <p>The other major part of the work was the PerceptionMetrics GUI. The existing GUI had three tabs:</p> <ul> <li>Dataset Viewer</li> <li>Inference</li> <li>Evaluator</li> </ul> <p>This worked for image detection, but it would become messy once image segmentation and LiDAR segmentation were added. Each task needs different dataset inputs, model inputs, visualizations, and evaluation options.</p> <p>To make this easier to extend, I started separating the GUI by task. The structure now moves toward folders like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tabs/tasks/image_detection/
tabs/tasks/image_segmentation/
tabs/tasks/lidar_segmentation/
</code></pre></div></div> <p>Each task can then have its own:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sidebar.py
dataset_viewer.py
inference.py
evaluator.py
</code></pre></div></div> <p>This keeps image detection code separate from segmentation code. It also gives us a cleaner place to add LiDAR visualization and evaluation later.</p> <h2 id="preserving-the-image-detection-workflow">Preserving the Image Detection Workflow</h2> <p>Before adding new segmentation behavior, I made sure image detection still behaved like the original GUI. I moved the existing image detection dataset viewer, inference tab, evaluator tab, and sidebar into the new task-specific folder while keeping the implementation close to the original version.</p> <p>This was important because the GUI refactor should not silently change the existing detection workflow. The goal was to first move the code into a better structure, not redesign everything at once.</p> <p>The image detection viewer still supports COCO and YOLO-style datasets. The sidebar remains responsible for dataset selection, model loading, configuration, and evaluation parameters.</p> <h2 id="image-segmentation-dataset-viewer">Image Segmentation Dataset Viewer</h2> <p>After the task split, I started implementing the image segmentation side with Cityscapes first. Other datasets such as nuImages, Wildscenes, RUGD, Rellis3D, and GOOSE are still planned, but Cityscapes was a good starting point because the tutorial already gives a clear reference workflow.</p> <p>The first implemented part was the Cityscapes dataset viewer. It loads a Cityscapes dataset using the dataset root, split, image directory, label directory, image suffix, label suffix, and train-ID option from the sidebar.</p> <p>It then displays samples and overlays the ground-truth segmentation mask on the image. This helped check that the GUI can read the same Cityscapes structure used in the tutorial.</p> <p>I also added a shared image grid utility for the GUI. The image detection viewer already had a paginated grid with search, and the image segmentation viewer needed the same behavior. Instead of duplicating it, I moved the grid code into a shared utility.</p> <p>This shared grid handles:</p> <ul> <li>pagination;</li> <li>search by sample name;</li> <li>image thumbnail selection;</li> <li>Streamlit session-state keys for each task.</li> </ul> <p>The detection and segmentation viewers can now reuse the same grid while keeping their task-specific rendering separate.</p> <h2 id="challenges">Challenges</h2> <p>The main challenge during these weeks was avoiding unnecessary duplication without over-abstracting too early. The GUI needs shared pieces, such as the image grid, but the actual task behavior should stay separate. Image detection, image segmentation, and LiDAR segmentation should not be forced into one generic viewer because their data and visualizations are different.</p> <p>Another challenge was understanding where ontology translation should happen. Exporting datasets to a new ontology is still useful, but evaluation can also use the translation directly when converted labels do not need to be stored.</p> <h2 id="next-steps">Next Steps</h2> <p>The next step is to continue the image segmentation GUI work by implementing the inference tab and evaluator tab for Cityscapes. This should follow the tutorial pattern by using a <code class="language-plaintext highlighter-rouge">TorchImageSegmentationModel</code>, a model configuration file, and an ontology file.</p> <p>After that, I plan to add image segmentation evaluation support in the GUI. Once the Cityscapes flow is working end to end, the same structure can be extended to other image segmentation datasets and later to LiDAR segmentation. I will also be exploring how to view open3d point clouds in the GUI for LiDAR segmentation.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="gui"/><category term="segmentation"/><category term="cityscapes"/><category term="nuimages"/><category term="semantickitti"/><summary type="html"><![CDATA[Improving segmentation evaluation with ontology translation and starting the PerceptionMetrics GUI task refactor]]></summary></entry><entry><title type="html">Coding Period Week 3</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-3/" rel="alternate" type="text/html" title="Coding Period Week 3"/><published>2026-06-14T00:00:00+00:00</published><updated>2026-06-14T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-3</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-3/"><![CDATA[<p>The third week of the coding period focused on improving the PerceptionMetrics documentation and preparing a nuImages image segmentation tutorial. After working on SemanticKITTI support and the LiDAR segmentation tutorial last week, I shifted focus to making the existing PerceptionMetrics workflows easier to understand and documenting the steps needed to evaluate an image segmentation model on nuImages.</p> <h2 id="pull-requests">Pull Requests</h2> <p>The main pull requests related to this week’s work are:</p> <ul> <li>nuImages tutorial: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/599">JdeRobot/PerceptionMetrics#599</a></li> <li>Documentation tabs: <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/602">JdeRobot/PerceptionMetrics#602</a> <h2 id="documentation-improvements">Documentation Improvements</h2> </li> </ul> <p>This week, I reviewed the existing PerceptionMetrics documentation to understand what was already available and what still needed to be explained more clearly. The project already has structured documentation and autogenerated Python API pages.</p> <p>After discussing the documentation structure with the mentors, we agreed that separate documentation tabs for datasets, models, metrics, and other major workflows would make the documentation easier to navigate. Based on that, I worked on adding more human-readable documentation pages for the main parts of the library. This included documentation for datasets, models, and metrics. The goal was to explain what is currently supported in the codebase, how the components are connected.</p> <p>For the dataset documentation, I focused on describing the available dataset adapters and how they fit into the PerceptionMetrics evaluation workflow. I also checked which datasets are already connected to the CLI and which ones still need clearer CLI support or documentation.</p> <p>For the model documentation, I documented the available model wrappers and clarified how model registration works in PerceptionMetrics.</p> <p>I also added a metrics documentation page. This explains the segmentation metrics reported by PerceptionMetrics, including precision, recall, accuracy, F1-score, IoU, and Dice score.</p> <h2 id="nuimages-segmentation-tutorial">nuImages Segmentation Tutorial</h2> <p>The second major task this week was preparing the nuImages image segmentation tutorial. As suggested by David, I fine-tuned a SegFormer model on the nuImages dataset and uploaded the trained checkpoint to <a href="https://huggingface.co/tejasstanley/segformer-nuimages-b0-full-bs8/tree/main">Hugging Face</a>. I then used this model in the tutorial and connected it to the PerceptionMetrics image segmentation evaluation workflow.</p> <p>The tutorial covers loading the nuImages dataset through the PerceptionMetrics adapter, preparing the model configuration, loading the segmentation model, running predictions, and evaluating the results.</p> <p>I created a model ontology that matches the labels predicted by the trained model. I also created an evaluation ontology with an <code class="language-plaintext highlighter-rouge">ignore</code> class so that classes not predicted by the model, such as <code class="language-plaintext highlighter-rouge">background</code> and <code class="language-plaintext highlighter-rouge">vehicle.ego</code>, can be handled properly during evaluation.</p> <p>The tutorial exports the dataset labels into the evaluation ontology before running metrics. I added prediction inspection steps to the notebook. These print the full set of classes predicted by the model and the classes expected by the dataset, which helps debug ontology mismatches. I added visualization of the input image, prediction, and ground-truth label side by side as expected in a typical image segmentation tutorial.</p> <p>The model initialization and dataset initialization are now separated into different cells, and I added markdown explanations around the ontology export, model wrapper setup, prediction inspection, and evaluation steps.</p> <p>Alongside this, David reviewed and merged my commit for Cityscapes. This helped close one more part of the image segmentation dataset work and gave me a clearer reference for how the nuImages workflow should fit with the rest of the project.</p> <h2 id="challenges">Challenges</h2> <p>The main challenge this week was deciding how much detail should go into documentation pages and how much should stay in tutorials. The documentation pages should explain the supported components and expected formats, while tutorials should show a complete workflow with real examples.</p> <p>Another challenge was handling the ontology mismatch in the nuImages tutorial. The model and dataset do not use exactly the same label space, so I had to carefully set up the model ontology, evaluation ontology, ignored classes, and translation files.</p> <h2 id="next-steps">Next Steps</h2> <p>Next week, as suggested by David, I plan to look into the evaluation function and explore how it can work directly with ontology translation without requiring a separate dataset export step. This would make the evaluation workflow cleaner, especially for datasets where the model and dataset ontologies do not match exactly.</p> <p>Another goal for next week is to explore GUI support for LiDAR segmentation. I plan to look for bugs in the current GUI workflow and take support from Praveena to refine the implementation and develop support for LiDAR segmentation evaluation.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="documentation"/><category term="nuimages"/><category term="segmentation"/><summary type="html"><![CDATA[Improving PerceptionMetrics documentation and preparing the nuImages image segmentation tutorial]]></summary></entry><entry><title type="html">Coding Period Week 2</title><link href="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-2/" rel="alternate" type="text/html" title="Coding Period Week 2"/><published>2026-06-06T00:00:00+00:00</published><updated>2026-06-06T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-2</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Tejas_Stanley/blog/2026/coding-period-week-2/"><![CDATA[<p>The second week of the coding period focused on adding SemanticKITTI support to PerceptionMetrics and preparing a tutorial around it. After studying the dataset structure last week, I started implementing a dataset adapter and building a workflow for loading LiDAR point clouds, reading semantic labels, running model inference through MMDetection3D, and using the results with PerceptionMetrics.</p> <h2 id="semantickitti-dataset-adapter">SemanticKITTI Dataset Adapter</h2> <p>This week, I worked on the SemanticKITTI dataset adapter, which is tracked in <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/588">JdeRobot/PerceptionMetrics#588</a>. Each SemanticKITTI sample contains a <code class="language-plaintext highlighter-rouge">.bin</code> point cloud file and a corresponding <code class="language-plaintext highlighter-rouge">.label</code> file, organized by sequence.</p> <p>The goal was to make the adapter follow the existing PerceptionMetrics dataset structure while correctly handling the SemanticKITTI format. I focused on loading point cloud files, matching them with their label files, and preparing dataset entries that can be used by the evaluation pipeline.</p> <p>I also worked on the SemanticKITTI ontology. This is needed because the raw dataset labels must be mapped to semantic classes before evaluation. For LiDAR semantic segmentation, this step is especially important because labels are stored in an encoded format and need to be decoded before they can be compared with predictions.</p> <h2 id="semantickitti-tutorial">SemanticKITTI Tutorial</h2> <p>Alongside the adapter, I started preparing the SemanticKITTI tutorial, which is tracked in <a href="https://github.com/JdeRobot/PerceptionMetrics/pull/590">JdeRobot/PerceptionMetrics#590</a>. The tutorial explains how to set up the dataset, load samples through the adapter, inspect the ontology, run inference, and prepare the data for evaluation.</p> <p>It also documents the dataset structure and explains how point clouds and labels are paired. This is useful because SemanticKITTI is less straightforward than many image segmentation datasets, especially for users who are new to LiDAR semantic segmentation.</p> <p>Writing the tutorial while developing the adapter helped test the workflow from a user perspective. Parts that were unclear in the tutorial often pointed to areas where the adapter or documentation needed improvement.</p> <h2 id="using-mmdetection3d">Using MMDetection3D</h2> <p>For the tutorial, I used MMDetection3D as the model-side framework for SemanticKITTI. MMDetection3D provides existing support for 3D perception models, pretrained configurations, and LiDAR semantic segmentation workflows. This allowed the tutorial to go beyond dataset loading and show how the adapter can be used in a practical model inference workflow.</p> <p>I explored how to load a SemanticKITTI-compatible model from MMDetection3D, run inference on point cloud samples, and connect the predicted semantic labels back to the PerceptionMetrics workflow. In this setup, the adapter provides the dataset structure and ontology, while MMDetection3D provides model predictions that can later be evaluated using PerceptionMetrics.</p> <p>This also helped identify the information that needs to stay aligned between the two libraries: point cloud data, predicted labels, ground-truth labels, and class mappings.</p> <p>The tutorial now shows a complete workflow where users can load data with the PerceptionMetrics adapter, use MMDetection3D for inference, and prepare the outputs for evaluation.</p> <h2 id="challenges">Challenges</h2> <p>The main challenge this week was correctly handling the SemanticKITTI format. The dataset uses sequence-based folders, binary point cloud files, and encoded label files, so the adapter needs to handle file matching and label decoding carefully.</p> <p>Another challenge was deciding what should be part of the adapter and what should remain in the tutorial or future model integration work. For now, I kept the adapter focused on loading and organizing the data, while the tutorial explains how to use it with MMDetection3D and PerceptionMetrics.</p> <h2 id="next-steps">Next Steps</h2> <p>Next week, I plan to focus on documentation improvements. This includes refining the documentation for existing adapters, models, functionalities and evaluation workflows existing in PerceptionMetrics. Clear documentation is crucial for making the library accessible to new users.</p> <p>I also plan to start exploring nuImages model training. This will help prepare for the next stage of dataset and model integration work, especially for understanding which pretrained or trainable models can be used with nuImages and how they can fit into the PerceptionMetrics evaluation pipeline.</p> <p>Another goal is to check the feasibility of using Vispy for point cloud visualization in the tutorial. This will be important for showing users how to visualize SemanticKITTI point clouds and predicted labels effectively. Comparing Vispy with Open3D will help determine the best approach for visualization in the tutorial.</p>]]></content><author><name></name></author><category term="gsoc"/><category term="gsoc"/><category term="robotics"/><category term="perceptionmetrics"/><category term="semantickitti"/><category term="lidar"/><summary type="html"><![CDATA[Implementing the SemanticKITTI dataset adapter and preparing the tutorial with MMDetection3D and PerceptionMetrics]]></summary></entry></feed>